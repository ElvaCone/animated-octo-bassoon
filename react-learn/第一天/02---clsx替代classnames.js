import classNames from 'classNames';

const className = classNames('btn', {
    'btn-primary': isPrimary,
    'btn-large': size === 'large',
});


import clsx from 'clsx';

const className2 = clsx('btn', {
    'btn-primary': isPrimary,
    'btn-large': size === 'large',
});
